# Visual QA — 午夜技术刊物重构

## 核验范围

2026-08-14 对首页、文章索引、文章详情和归档页进行了桌面及移动视口检查。页面已统一为墨黑纸感背景、冰蓝信号色、中文衬线标题和等宽元信息的“午夜技术田野札记”视觉语言。

| 页面 | 桌面端 | 移动端 | 结果 |
|---|---|---|---|
| 首页 `/` | 非对称 Hero、观测站主图、内容索引正常 | 标题换行、主图裁切、底部信息正常 | 通过 |
| 文章索引 `/posts` | 筛选侧栏与档案条目正常 | 筛选与条目单列正常 | 通过 |
| 文章详情 `/posts/ddd` | 标题、元信息、封面、正文与评论结构正常 | 文章头部和阅读区单列正常 | 通过 |
| 归档 `/archives` | 月度编目结构正常 | 月度记录与统计单列正常 | 通过 |
| 标签 `/tags` | 术语索引结构正常 | 空状态与长标题保持单列可读 | 通过 |
| 关于 `/about` | 宣言、技术栈和原则分区正常 | 中文长文、技术条目与规则线无溢出 | 通过 |
| 图片集 `/gallery` | 空状态不伪造样例内容 | 图片柜空状态、说明文字与图标正常 | 通过 |
| 不存在图片集 `/gallery/1` | `CABINET NOT FOUND` 与返回图片集按钮正常 | 错误标题保持可读，返回按钮可触达 | 通过 |

## 运行时确认

浏览器直接访问文章索引与 `/posts/ddd` 均得到正常内容响应。截图工具在服务重启后首次截取详情页时可能捕获到“LOADING TRANSMISSION…”过渡状态；浏览器提取确认请求完成后，详情包含分类、标签、封面与评论区域。

2026-08-14 复核图库：`/gallery` 在真实无图库数据时显示 `CABINET EMPTY`，并明确说明不会使用伪造样例内容；`/gallery/1` 的 NOT_FOUND 响应关闭自动重试后，会在桌面和手机端立即显示可操作的返回图片集入口，不会持续停留于加载画面。

数据库核对：同日执行 `SELECT COUNT(*) AS gallery_count FROM galleries;`，返回 `gallery_count = 0`。因此上述 `/gallery` 空状态验收对应当前真实数据库状态，而非人为构造的空白数据。

运行时网络核对：开发服务器日志记录 `/gallery` 发起的 `galleries.list(page=1, limit=20)` 请求返回 HTTP `200`，首个 tRPC 结果为 `json: []`（最近一次记录于 2026-08-14 16:24:16）。这与数据库 `gallery_count = 0` 一致，并由 `GalleryPage.tsx` 的空数组分支渲染 `CABINET EMPTY`。桌面和手机截图分别确认该空状态采用单列/适配后的档案式布局，且不出现伪造卡片。

## 站内邮箱认证验收

2026-08-15 对 `/login` 与 `/login?mode=register` 进行桌面和手机端核验。登录与注册被收纳为同一页面的两个可切换标签；默认登录视图只保留邮箱和密码，注册视图依次呈现昵称（可选）、邮箱、六码验证码、密码和确认密码，不把无关字段塞入登录路径。

| 路径 | 桌面端 | 手机端 | 结果 |
|---|---|---|---|
| `/login` | 左侧说明身份边界，右侧保持紧凑的邮箱/密码登录表单 | 导航收缩，说明区与表单按阅读顺序垂直排列 | 通过 |
| `/login?mode=register` | 验证码输入与“获取”按钮并列，密码确认在主提交动作前 | 验证码输入与获取按钮保持同一触控行，其余字段单列且无横向溢出 | 通过 |

运行时验证：`auth.requestRegistrationCode` 将注册表单填写的邮箱作为邮件 `to` 字段，163 SMTP 账号只作为服务端发件配置，不参与访客身份或权限判断。此前用于连通性测试的发件邮箱验证码记录已删除。重复请求在 60 秒冷却期内返回 `TOO_MANY_REQUESTS`，前端会以 toast 呈现服务端错误；密码不一致在提交前于客户端提示，不会调用注册接口。注册成功后的 Cookie 签发与身份恢复由自动化路由和会话测试覆盖。

2026-08-15 真实站内账户验收：一名完成邮箱验证码注册的账户可进入作者工作区和写作工作台；重启服务后，该账户与独立 `INITIAL_ADMIN_EMAIL` 配置匹配并获得管理员角色。管理员会话已实际打开文章、审核、术语、图像和用户目录五个分栏；术语分栏提供标签/分类输入，图像分栏提供图片集创建入口，空审核队列与空图片集都给出明确状态，而非伪造示例数据。未在此记录中保存注册邮箱或验证码。

认证加载态：已在真实登录会话下重新进入作者工作区。身份请求尚未完成时页面仅显示 `OPENING YOUR INDEX…`，完成后进入工作区；不再短暂渲染“需要登录”或“未授权”内容。写作页采用同一加载顺序。

复核写作页：在同一真实登录会话中重新打开 `/create`。初始帧只显示 `OPENING WRITING DESK…`，随后进入可编辑的标题、Markdown 正文、图片插入、文档设置、草稿和发布操作界面；整个过程没有出现未授权提示。

术语与作者元数据：真实管理员会话在术语分栏创建了基础标签“开源”和分类“工程实践”，创建后索引即时刷新。真实作者会话重新打开 `/create` 的文档设置后，分类下拉框包含“工程实践”，标签按钮包含“#开源”，证明后台术语写入与写作元数据选择已连通。未为测试发布虚构文章或上传非用户提供的图片。

标签筛选：从公开 `/tags` 页面点击“#开源”后，地址栏正确进入 `/posts?tag=开源`，文章索引显示 `0 MATCHED` 与“没有找到匹配的记录”，不会错误回退为全量文章；点击 `RESET` 后地址恢复为 `/posts` 并重新显示现有文章。该验证基于真实后台创建的标签，未把任何测试文章伪造为带标签内容。

手机访客回退：以 375×812 视口复核 `/admin`、`/create` 和 `/posts?tag=开源`。未登录时前两者分别显示明确的管理员限制与授权要求页面；标签筛选空态保留筛选词、匹配数量、清除入口和移动端单列信息层级。独立的真实管理员登录态移动端验收仍列为待办。

搜索与分享状态：文章索引的搜索词已进入服务端分页查询。直接访问 `/posts?q=ddd` 时显示现有文章与 `1 MATCHED`；输入不存在的检索词后，地址栏同步为 `?q=...` 并显示 `0 MATCHED` 与可操作的清除入口；清除后恢复 `/posts` 与完整索引。375×812 视口下，命中内容、筛选项和空态均保持单列可读，未出现横向溢出。

组合筛选恢复：桌面端直接访问 `/posts?q=ddd&category=1`，搜索框恢复 `ddd`、真实“工程实践”分类保持选中，结果按组合条件显示 `0 MATCHED` 与清除入口；同一路径在 375×812 视口下保持筛选层级、选中标记与空态可读。客户端 URL 状态纯函数另有自动化测试，覆盖搜索、标签、分类及重置后的规范路径。

公开页面手机复核：以 375×812 视口重新检查 `/tags`、`/about` 和 `/gallery`。标签索引以单列术语条目呈现，记录计数与进入箭头保持可触达；关于页的长文本、技术栈与原则分区无横向溢出；图库在真实零数据状态显示 `CABINET EMPTY` 及明确的非伪造内容说明。三页均保留简化导航、清晰标题层级和完整页脚。

阅读时长：文章索引与详情页基于实际 `content` 字段显示 `01 MIN READ`。在 `/posts/ddd` 的桌面端，阅读时长位于日期、浏览量和评论数之间；在 375×812 视口中，索引卡片与详情头部的时长图标、文本和其他元信息保持单列阅读节奏且无截断。时长计算另有中英文、Markdown 链接和空正文边界的自动化测试。

可读性与界面语言重设计：本轮将基础背景改为可区分的深蓝灰层次，提升正文、次级文字、输入框边界、焦点态与按钮的对比度；按钮、文章卡片、筛选器、登录表单、写作画布和文章阅读面板统一为柔和圆角与分层表面。首页、文章列表、详情、登录和写作页删除了“档案信号”“传输”等模板化术语，改为面向访客的自然中文提示。桌面端检查 `/`、`/posts`、`/posts/ddd`、`/create`，375×812 检查 `/`、`/posts`、`/posts/ddd`、`/login`、`/create`；页面无横向溢出，主要标题、正文、筛选项、输入框和操作入口均可辨识。

按需路由加载：除首页外的文章列表、详情、写作、登录、管理、图库、归档、标签与关于页面均改为路由级惰性加载，首次进入非首页时显示“正在打开页面… / 内容马上就好。”的中性中文回退。生产构建确认主入口脚本降至 665.25 kB（gzip 195.72 kB），文章列表、详情、写作、管理与登录分别成为独立页面模块。重启开发服务后直接访问 `/posts` 显示真实文章、分类和标签；直接访问 `/posts/ddd` 可完成加载并显示文章正文、元信息及评论入口。

真实作者工作台补验：在当前已登录的管理员兼作者会话中，直接打开 `/create` 后完成授权加载并显示标题、正文、编辑/预览、存草稿和发布操作。打开“设置”后，抽屉显示文章链接、摘要、真实分类“工程实践”、真实标签“#开源”、封面图片上传入口及图片 URL 输入框。未填写、保存或发布任何演示性文章，也未上传非用户主动提供的图片；因此创建、发布和真实图片写入仍保留为内容数据依赖项。

真实作者工作区补验：同一会话直接访问 `/workspace` 成功进入作者私有工作区，显示“新建记录”与“进入公开文章索引”入口；当前账号没有自己的草稿或已发布文章，因此“待续写”和“已发布”分区准确呈现为零记录空状态。现有公开测试文章归属于另一作者，未被错误显示为当前作者内容。

作者工作区语言重构：在真实会话中重新打开 `/workspace`，页面已改为“我的文章”“草稿”“待续写”“已发布”“公开文章”等自然中文层级；两个真实零记录状态分别显示“还没有待续写的草稿”和“还没有发布自己的文章”。新文章与公开文章列表入口可操作，圆角分区、计数徽标和虚线空态均在桌面端正常显示。

管理控制台语言重构：在真实管理员会话中重新打开 `/admin`，页面已使用“管理后台”“把内容，好好照看”“内容管理”等自然中文；文章、审核、标签与分类、图片集、用户五个分栏全部可见。默认文章分栏显示真实已发布文章、阅读次数、查看和删除操作；顶栏、分栏控件、计数徽标和操作卡片均使用圆角分层与高对比度文字。

公开页面语言重构：归档、标签、图库、图库详情和图片上传控件已清理剩余的英文档案术语。归档与标签改为自然中文加载、空状态和计数；图库明确说明只展示真实上传的图片；缺失图库详情显示友好的中文解释和“返回图片集”入口，不再暴露 `NOT_FOUND` 等底层错误代码。桌面与 375×812 视口均复核 `/archives`、`/tags`、`/gallery`、`/gallery/999999`，其单列卡片、中文文案、真实空状态和返回动作均可读、可触达且无横向溢出。

顺序播放与实时歌词：站点所有者确认可使用 `morro.asia` 项目的音乐及 LRC 后，播放器已接入七首曲目的顺序列表。真实桌面会话中主动点击“播放歌单”后，底部播放器显示《Love Song》与当前歌词；点击“下一首”后即时切换为《红尘客栈》，曲目、歌词和“第 2 / 7 首”编号同步变化。移动端 375×812 视口复核了首页与播放器触发入口的导航密度和安全边距；播放器采用底部浮层，保留上一首、播放/暂停、下一首、收起与实时歌词区域。浏览器初始访问不自动播放，启动后每首结束会顺序进入下一首。

## 高星开源项目研究驱动的体验复核

2026-08-15 基于 Ghost、Tiptap、Hexo 与 Hugo PaperMod 的公开项目/演示研究，已将可迁移的结构原则落实为“单一主任务、可扫描元信息、链接可恢复筛选、正文优先写作和手机端独立操作布局”。研究依据与不复制视觉成品的边界记录于 `DESIGN_RESEARCH_HIGH_STAR.md`。

| 页面 | 桌面端（1280×720） | 手机端（375×812） | 结果 |
|---|---|---|---|
| 首页 `/` | 首屏仅保留写作与阅读两条主路径；“可以从这里开始读”紧接真实最近文章，归档/标签/图片集成为低干扰的继续探索入口 | 首屏按文案、动作、主图、真实文章的阅读顺序排列；探索入口为独立触控行 | 通过 |
| 文章索引 `/posts` | 筛选面板与文章清单并列，标题、摘要、日期、阅读时长和阅读数在同一卡片内可扫描；筛选链接提示明确 | 默认收起标签和分类，保留“筛选”真实开关；文章卡单列、不出现横向溢出 | 通过 |
| 文章详情 `/posts/ddd` | 保留返回索引、标题元信息、作者/标签旁栏、单栏正文和评论的连续阅读节奏 | 头部、作者信息、正文和评论按自然阅读顺序单列；真实文章 `ddd` 已完成加载、封面与评论空态可见 | 通过 |
| 写作页 `/create`（未登录回退） | 登录提示保持一个明确返回动作，不展示无功能编辑控件 | 认证回退卡片居中、触控按钮可达；未引入假表单或假草稿 | 通过 |

本轮同时为写作台补充了真实“未保存的更改 / 草稿已保存 / 已发布，尚无更改”状态基础，避免旧版编辑已有文章时一律显示“尚未保存”的错误暗示。正文字符数改按去除空白后的实际字符显示，编辑区拥有可访问名称；手机端文档设置在样式契约中改为底部抽屉，宽屏则保持右侧面板，避免用缩小桌面抽屉代替手机流程。固定歌词播放器打开后，主内容区域会新增底部安全留白，避免遮挡长文末尾和评论入口。

## 可读性结论

## morro.asia 设计逻辑参考后的原创夜蓝重构

2026-08-16 以 `morro.asia` 真实首页为研究参考，提炼其“丰富背景明度、少量透明环境层、分散但有呼吸感的信息层级、关键入口数量有限”的设计逻辑；未复制其插画、人物、头像、图标、时钟天气、文案、品牌或版式。研究边界和可迁移原则见 `DESIGN_RESEARCH_MORRO_ASIA.md`。

| 页面 | 桌面端 | 375×812 手机端 | 验收结果 |
|---|---|---|---|
| 首页 `/` | 用深蓝灰夜色与真实观测站图建立单一视觉锚点；标题、说明、写作/阅读入口和最近文章以连续分割线组织，不再使用黑灰大卡片堆叠 | 标题、动作、图像和真实文章按一列阅读顺序排布；归档、标签、图库为独立可触达行 | 通过 |
| 文章索引 `/posts` | 信息区、筛选、结果数量和文章行由细线与留白组织；真实文章 `ddd`、真实“开源”标签和真实“工程实践”分类在请求完成后正常显示 | 筛选默认收起，正文与文章行保持高对比且无横向溢出；真实文章封面、阅读时长与分页可见 | 通过 |
| 文章详情 `/posts/ddd` | 标题、时间/时长/阅读量、封面、侧边信息、正文与评论改为连续阅读面，不使用厚边框内容面板 | 返回入口、元信息、封面、作者/标签、正文和评论沿单列自然排列；正文与背景明度可区分 | 通过 |
| 全局导航与播放器 | 顶部改为夜蓝半透明环境层、下划线导航和细线品牌标记；播放器改为较亮的夜蓝信息条，保留所有原有控制与歌词功能 | 窄屏菜单为双列文字入口，登录与菜单控件清晰可点；播放器打开时主内容自动增加底部安全留白 | 通过 |

运行时说明：预览环境的文章索引首帧会呈现加载骨架，等待 tRPC 请求完成后才显示当前真实文章、术语和计数；验收结论以完成态为准。全量测试结果为 **26 个测试文件、68 项通过、1 项显式 SMTP 集成测试跳过**；`pnpm check` 与 `pnpm build` 均通过。

写作工作台补验：在真实已登录管理员兼作者会话中直接访问 `/create`，标题、正文输入区、编辑/预览切换、图片插入、文档设置、存草稿及发布入口均正常呈现；本轮将其近黑标题卡、工具栏、正文框与设置抽屉同步为夜蓝细线结构。为遵守不伪造内容要求，未输入、保存或发布演示性文章，也未上传图片。

管理员补验：在同一真实管理员会话中打开 `/admin`，默认文章分栏显示当前真实文章及查看/删除操作；切换“标签与分类”分栏后，真实“#开源”和“工程实践”均可见，新建标签/分类输入与添加入口可操作。为避免污染真实内容，本次未提交任何新术语、删除或审核操作。

图片集补验：管理员“图片集”分栏显示当前真实的 0 条状态、名称/说明输入、创建入口和“选择一个图片集”空状态。入口未被禁用；但在没有用户提供的真实图片集名称、说明和图片文件时，未创建任何示例图片集或上传任何内容。

手机端公开回退补验：以 375×812 访问未登录的 `/create` 时，页面不显示不可用表单，而是提供“登录后即可写文章”的清晰说明与返回首页入口；该回退与手机端顶部导航保持可读。由于截图环境未承载真实登录 Cookie，已登录作者的手机端提交动作仍保留为待真实会话验收项。

管理后台手机端公开回退补验：以 375×812 访问未登录的 `/admin` 时，不渲染受限后台控件，而是显示“这里仅供管理员使用”的权限说明与返回首页入口。该截图环境同样不携带管理员 Cookie，因此真实管理员手机端的内容管理与审核动作继续保留为待验收项。

访客优先导航补验：在真实登录管理员会话下访问首页，顶部始终保留首页、文章、归档、标签、图片集、关于和音乐等访客内容入口；作者工作区与“写文章”入口均不再出现。管理员只看到“管理员视角”和退出；首页主动作同步变为“进入管理员视角”与“浏览文章”。

图片集管理员工作流补验：在真实管理员会话中打开“图片集”分栏，确认图片集目录、创建图片集名称/说明字段、当前选择的说明区域和按选择解锁的上传/整理工作区均可到达。当前数据库真实图片集数量为 0，因此界面诚实显示“先选择一个图片集”与下一步说明；未创建示例图片集、未上传测试图片。

认证文案与验证码提示补验：桌面与 375×812 注册页均显示“用邮箱，确认是你”的具体说明、账号安全提示、邮箱验证码用途和 10 分钟时效说明。手机端“发送验证码”改为紧凑单行“发送”标签，并保留完整的无障碍名称，避免原有文字纵向折行。

正文与背景保持高亮度差，交互与强调色限制为冰蓝和低饱和珊瑚色。取消紫色大渐变、发光玻璃卡、连续粒子和自动装饰动效。图片均使用语义化替代文本；文章封面在没有有效存储 URL 时回退到已核验的真实观测/夜景图。

## 仍需内容侧完善

当前数据库仅有一篇标题为 `ddd` 的测试文章，视觉并不应掩盖这一内容事实。建议后续通过后台发布真实文章、摘要、分类、标签和封面图；界面不应预置或伪造用户内容来填充版面。

## 全站自然中文文案统一

2026-08-16 已对首页、文章索引、文章详情与评论、归档、标签、图库与图库详情、关于、写作工作台、作者工作区、管理员后台、认证、全局导航/页脚和默认 404 页面做可见文案审阅。改写原则为：说明真实用途和下一步操作，避免口号化、机械化英文和虚构状态；不以示例文章、评论或图片填补内容空白。

| 路径或区域 | 桌面端 | 375×812 手机端 | 结果 |
|---|---|---|---|
| 首页 `/` | 首屏改为“把用过、查过的，写清楚”，最近文章、归档、标签和图库入口均使用具体说明 | 标题、说明、文章条目、探索入口与页脚按单列阅读，文案无截断 | 通过 |
| 文章索引 `/posts` | 搜索、筛选链接、结果状态和无摘要回退明确说明实际行为 | 筛选、标题、文章元信息和分页保持可读 | 通过 |
| 归档、标签、图库 | 分别明确按月回看、按主题筛选和只展示真实上传图片的边界 | 图库真实零数据状态不出现示例图；所有空状态与说明可读 | 通过 |
| 关于 `/about` | 站点说明、写作原则、技术和维护信息改为具体中文 | 提升说明文字至 `#c7cbd0`，避免低明度正文在手机端难以阅读 | 通过 |
| 全局与默认异常页 | 品牌副标题、页脚与播放提示改为实际阅读/维护说明；404 不再是英文浅色模板 | 导航、页脚和异常页保留清晰中文返回路径 | 通过 |

质量验证：`pnpm check` 通过；Vitest **29 个测试文件、77 项通过，1 项显式 SMTP 集成测试跳过**；`pnpm build` 通过。截图复核覆盖桌面 `/`、`/posts`、`/archives`、`/tags`、`/gallery`、`/about` 及 375×812 的 `/`、`/posts`、`/gallery`、`/about`。

## 右侧导航与悬浮音乐球升级

2026-08-17 已将公共入口保留为始终可见的导航，同时调整为宽屏右侧排列；手机端则保留独立的六列触控行。顶部不再提供音乐入口，避免与站点导航竞争注意力；音乐仅由右下角悬浮球承载。

| 验收区域 | 桌面端（1280×720） | 手机端（375×812） | 结果 |
|---|---|---|---|
| 右侧公共导航 | 首页、文章、归档、标签、图片集、关于在品牌右侧连续排列，登录保持在最右侧 | 品牌和登录后紧接六列栏目行，所有栏目无需展开菜单即可触达 | 通过 |
| 收起态悬浮音乐球 | 右下角显示原创抽象曲目封面、曲目编号、播放状态和环形进度，不遮挡首屏主动作 | 球体位于右下安全边距，尺寸适合单手触达且不压住主标题或登录动作 | 通过 |
| 展开态播放面板 | 点击音乐球后显示当前封面、曲目/歌手、实时歌词、进度拖动、上/下一首、播放暂停和七首歌单 | 面板宽度随视口收缩，选曲列表保留可滚动区域和清晰触控行 | 通过 |

运行时交互核验：预览环境先从收起态音乐球打开完整面板，成功读取《Love Song》的 LRC 当前句和时长；随后选择《红尘客栈》，曲目编号、标题、歌手、抽象封面、歌词首句和时长均同步切换到第 2 / 7 首。浏览器初次进入未触发自动播放；音乐开始仍需访客在面板中主动点击播放。曲目封面采用原创抽象色块和文字标记，未伪造或挪用不存在的专辑封面资源。

## 三层悬浮播放器精细化

2026-08-17 将播放器进一步改为渐进三层。一级为贴合真实封面与歌曲名的微型磨砂胶囊；触碰、悬停或键盘聚焦后进入二级直接控制条；三级才显示实时歌词、进度、选曲和完整控制。七首曲目封面均从用户已授权的 `morro.asia` 音频文件的真实内嵌图片中提取并作为站点静态资源引用，未生成或伪造封面。

| 验收区域 | 桌面端（1280×720） | 手机端（375×812） | 结果 |
|---|---|---|---|
| 一级闲置胶囊 | 右下仅显示真实圆形封面与《Love Song》名称，尺寸贴合内容、无嵌套留白 | 位于右下安全边距，未压住首屏标题、登录或文章操作 | 通过 |
| 二级控制条 | 在同一浮层内展开播放暂停、上下切歌与低干扰时间线；外部点击才收回一级 | 控制与进度触控区保留最小可达空间，收起态不堆叠歌词 | 通过（组件状态与回归契约覆盖） |
| 三级完整面板 | 返回动作固定回到二级控制条；一级/二级与三级持续挂载，通过透明度、位移和缩放完成进入/退出 | 完整面板继承当前浮条坐标展开；计算位置在边缘自动限位，避免面板超出视口 | 通过（类型检查、回归契约与跨端静态复核） |

动画策略：层级尺寸、透明度和阴影使用短时 `cubic-bezier(0.23, 1, 0.32, 1)` 过渡；三级面板保留退出状态而不是条件卸载；拖动期间禁用非必要过渡；全站的减少动态效果媒体查询继续生效。质量验证为 `pnpm check` 通过；Vitest **34 个测试文件、86 项通过，1 项 SMTP 集成测试跳过**。

## 全站体验、账号恢复与时间轴升级

2026-08-17 完成本轮桌面 1280×720 与手机 375×812 验收。时间轴、找回密码和未登录保护路径均基于当前真实数据与权限状态；未发送真实重设邮件、未修改账号密码、未创建虚构文章或图片。

| 路径 | 桌面端 | 手机端 | 结果 |
|---|---|---|---|
| `/timeline` | 新增“沿着时间，回到写下它的那天”阅读首屏；真实公开文章按年份与发布日期沿纵向轨道排列，标题、摘要、阅读量和原文入口均可见 | 七列栏目仍保持可触达；首屏、年份、日期节点与文章信息按单列展开，无横向溢出 | 通过 |
| `/login?mode=reset` | 账号恢复说明与表单双栏分离；邮件验证码、新密码、确认密码和通用账号隐私提示清晰 | 说明先于表单自然纵向排列，安全提示、返回登录和字段顺序连续，适合单手滚动完成 | 通过 |
| `/create`（未登录） | 仅显示明确登录回退，不显示不可提交的编辑器 | 标题、说明和返回按钮保持可读可触达，底部页脚与播放器不遮挡主操作 | 通过 |
| `/admin`（未登录） | 仅显示管理员权限回退，不渲染受限内容控件 | 权限说明和返回首页动作保持单列清晰 | 通过 |

运行时与构建验证：密码重设流程的界面入口、验证码用途隔离、重设后新会话签发及旧本地会话失效由认证测试覆盖；全量 Vitest 为 **40 个文件、101 项通过，1 项 SMTP 集成测试跳过**。生产构建通过；共享数据、图标、动效和 Markdown 渲染已拆分为独立缓存块，歌词请求在换曲或卸载时使用 `AbortController` 取消，播放器进度状态更新节流至不高于约每 120ms 一次。

## 访客首页的个人展示与最新文章入口

2026-08-17 以 1280×720 和 375×812 复核首页 `/`。宽屏首屏采用左侧个人叙事、右侧公开一言的两列结构：左侧首先呈现 Morro、个人技术记录者、开源实践/AI 工具/系统与硬件三个真实关注方向，以及“从最新记录开始”和“沿时间阅读”动作；右侧独立承载公开一言、来源与刷新操作。首屏之后立即进入“最新文章”，以当前真实公开文章作为主要继续阅读路径。

手机端按作者身份、介绍、关注方向、两个动作、公开一言、最新文章的自然阅读顺序单列重排。头像、标题、正文、一言卡、按钮和右下播放器之间均未出现横向溢出或遮挡。当前一言请求在截图时已返回真实来源“高阳台·西湖春感”；若网络尚未完成，界面仅显示“正在摘取一句话…”，不会补写内置短句。首页回归、可读性与品牌配置契约共 6 项通过。

## 右上常显节目入口

2026-08-18 吸收用户授权引用任务的“各个节目不要隐藏起来”结论，并按用户后续要求将宽屏公共栏目置于右上角。桌面端首页、文章、时间轴、归档、标签、图片集和关于常显于右上；品牌保留在左上，登录或管理员动作紧随栏目而不与访客阅读入口竞争。375×812 手机端保留七列常显栏目行，品牌与登录位于上方，不使用汉堡菜单隐藏任何公共栏目。两种视口下栏目均可见、无横向溢出，右下播放器仍保留安全边距与可触达区域。

## Halo 播放器研究驱动的状态韧性升级

2026-08-18 在不复制 Halo 插件的视觉、APlayer 依赖、Navidrome 服务、搜索抽屉或 PJAX 兼容层的前提下，完成 MorroBlog 两层播放器的状态韧性升级。桌面预览首页验证：一级磨砂播放条正常保持在右下安全边距；点击曲目信息区后直接进入三级完整面板，真实内嵌封面、曲名/歌手、LRC 当前句、精确进度、上/下一首、播放暂停和 7 首曲目列表均正常呈现。三级面板新增“定位当前”动作，可将长列表重新定位到当前曲目；当前 7 首曲目规模不会额外引入搜索或分页负担。

本轮新增本地播放上下文，保存并校验当前曲目、进度、暂停状态、音量和拖拽位置。恢复时会等待媒体元数据可用后再设置时间点，首访保持静默；若浏览器拒绝恢复播放，界面保持暂停而非把音乐错误标记为不可用。键盘方向键跳转与空格播放/暂停仅在三级面板开启且焦点不在输入、按钮、链接或编辑区域时生效；桌面预览中从“展开播放器”按钮直接按右方向键未触发跳转，符合按钮焦点保护预期。随后将焦点移至页面主体后按右方向键，一级与三级进度均从 `0:00` 同步更新为 `0:05`，确认正向跳转链路生效。随后刷新同一页面，一级播放条在保持暂停的情况下恢复为同一首《Love Song》与 `0:05 / 4:29`，确认恢复不触发自动播放且未出现进度回弹。窗口尺寸变化会重新将已拖拽的播放条和完整面板限位在可视范围。自动化回归已覆盖持久化输入校验、就绪后恢复、节流写入、页面离开保存、快捷键豁免和当前曲目定位；全量结果为 **42 个测试文件、106 项通过，1 项显式 SMTP 集成测试跳过**。手机端 375×812 的真实播放、恢复、拖拽与键盘路径将作为本轮的后续运行态复核项。

375×812 截图复核：首页的七列公共栏目保持可见；首屏标题、主动作与公开一言维持单列节奏。一级播放条停靠右下，不遮挡首屏的主要开始阅读动作；真实封面、曲目/歌手、已暂停状态、精简进度、时间和基础控制均保持可辨识，未出现横向溢出。该截图来自无真实浏览器会话的独立预览，因此只确认布局、安全边距与可读性；手机端的刷新恢复、拖拽和展开后的键盘行为仍不以静态截图替代运行态验收。

## Halo 同级播放器结构重构

2026-08-18 用户明确要求播放器采用 Halo Navidrome 同级的完整功能结构，而非仅做差异化借鉴。因此本轮将此前的强化一级信息条替换为可拖拽的 64px 悬浮封面入口；点击后进入主播放面板，面板内提供真实 LRC 当前/下一句、拖拽进度、上一首/播放暂停/下一首、音量、顺序循环/单曲循环/随机播放，以及“定位当前”“搜索”“节目单”三个全部有效的工具入口。歌单和搜索均只在现有七首已授权曲目上运行，不伪造额外节目单、音乐库或外部服务。

桌面初始运行态复核确认：悬浮入口位于右下安全边距，封面、序号和暂停标记可见，未遮挡首页主要动作；入口的无障碍名称准确表明曲目与当前暂停状态。等待 React 状态提交后，完整主面板正常展开：真实封面、曲名/歌手、LRC 当前与下一句、`0:05 / 4:29` 进度、基础控制、音量、播放模式、定位、搜索和节目单控件均可达。点击“搜索”后，左侧抽屉显示真实七首曲目与可编辑的歌名/歌手输入框，列表可滚动且当前曲目有明确标记；输入“周杰伦”即时收窄为四首真实匹配曲目。随后选择《红尘客栈》，搜索抽屉收起，主面板封面、曲名、歌手、LRC、序号和总时长同步切换为第 2 / 7 首与 `0:00 / 4:34`。主动点击播放后，状态变为“正在播放”，进度从 `0:00` 连续推进至 `0:08`，LRC 随时间更新；点击播放模式后无障碍标签由“顺序循环”切换为“单曲循环”。未伪造额外歌单或搜索结果。新版自动化结果为 **43 个测试文件、108 项通过，1 项显式 SMTP 集成测试跳过**。

375×812 静态复核：顶部七列公共栏目、作者叙事和公开一言保持手机端单列排布；64px 音乐入口位于右下安全边距，封面序号与状态标记清晰，入口保持在正文滚动层之上。固定入口会覆盖当前视口最下方的次级信息区域，但首页仍可继续滚动，不遮挡顶部栏目和首要“从最新记录开始”动作。

375×812 真实触控视口复核：通过浏览器设备模拟实际打开第 2 首《红尘客栈》的完整面板，面板定位为 `left: 12px`、`top: 290px`、`width: 351px`、`height: 279.75px`，完整落在 375×812 可视范围内。点击播放后暂停控件实际切换为可见，媒体保持 `paused: false` 且进度读数为 `117.99s`，确认移动端播放链路有效。打开搜索后，输入框获得焦点；修正抽屉空间策略后，抽屉位置为 `left: 83px`、`top: 12px`、`width: 280px`、`height: 266px`，不再越过上沿。真实触控拖拽将入口从 `left: 263px, top: 694px` 移至 `left: 227px, top: 652px`，确认整块悬浮入口的触控拖拽状态机有效；验收后已清除测试写入的位置并恢复默认右下停靠。该验证同时确认窄屏抽屉的可见范围、焦点转移、播放控制与拖拽；键盘快捷键继续由桌面运行态和回归契约覆盖。

刷新恢复复核：在桌面播放《红尘客栈》并切换至单曲循环后刷新页面，悬浮入口恢复为第 2 / 7 首《红尘客栈》与“已暂停”状态，未违反浏览器禁止无用户触发自动播放的限制。浏览器自动化的合成指针点击在当前页面会受拖拽捕获与异步状态提交影响，因此不以同一轮点击后的即时静态读数覆盖先前已确认的主面板完成态；当前曲目与暂停状态恢复已在运行态确认。

## 直接采用 Halo Navidrome Player 前端样式

2026-08-18 按用户明确要求，播放器改为直接加载 `queenyn/halo-plugin-navidrome-player` 提交 `af865b7` 的原始 MIT `navidrome.js` 与 `navidrome.css`，不再保留先前自研播放器的表面与面板结构。由于 MorroBlog 使用 React 路由，配置将上游 PJAX 设为 `off`，保留其悬浮气泡、主面板、APlayer 控件、当前/下一句歌词、节目单、搜索、定位、音量、循环模式、位置与播放状态持久化等原始前端行为。

桌面运行态确认：点击右下 `♫` 气泡后，原始面板加载《Love Song》真实封面、曲名、歌手、LRC 与七首节目单；通过上游列表选择第 2 首《红尘客栈》后，封面、标题、歌词、暂停图标和进度同步为该用户授权曲目。随后打开原始“搜索歌曲”抽屉，输入“周杰伦”仅返回《红尘客栈》《花海》《烟花易冷》《发如雪》四首真实匹配曲目。网络日志确认 `/apis/ext.navidrome/v1/playlist` 与 `/apis/ext.navidrome/v1/lyrics` 均返回 HTTP 200，歌词端点实际返回《红尘客栈》的 LRC 内容。

375×812 复核：首页继续保持七列公共栏目和单列阅读节奏；全页截图按固定元素隐藏规则不显示右下气泡，故不将其误判为缺失。原始上游样式已通过同一运行环境挂载，桌面交互验证覆盖曲目加载、主动播放、切歌、歌词和搜索；移动端页面布局截图确认未因脚本接入产生页面横向溢出。上游代码、APlayer 依赖、原始版权、MIT 许可证、冻结版本及 MorroBlog 的授权曲目适配边界均记录在 `THIRD_PARTY_PLAYER_ATTRIBUTION.md`。

## 05b1a7c 播放器定向恢复

2026-08-18 根据用户的明确指令，已将**仅音乐播放器**恢复至发布副本提交 `05b1a7c` 的三级悬浮交互：一级为右下的封面与曲名胶囊，点击进入二级控制条，二级再进入包含歌词、原生进度条、播放/暂停、上下切歌与七首真实曲目的三级完整面板。除播放器组件、播放器状态逻辑、专属样式、相关兼容路由、归属说明和测试契约外，博客其余功能未回退。

桌面运行态复核：首页初始显示《Love Song》一级胶囊；触发后，二级条显示真实封面、曲名/歌手、`0:00 / 4:29` 进度、上下切歌与播放控制。展开三级面板后，当前 LRC 行、原生时间范围输入、返回二级动作及完整七首曲目列表均可见。选择《红尘客栈》后，封面、曲名、歌手、曲目序号、当前歌词和时长同步变为第 `2 / 7` 首及 `0:00 / 4:34`。上游 Halo/Navidrome 加载器、兼容接口和运行时归属文件已解除；音频继续只来自用户授权的 `morro.asia` 七首曲目。

质量验证：`pnpm check` 通过；Vitest **43 个测试文件、106 项通过，1 项 SMTP 集成测试跳过**；`pnpm build` 通过。此前 Halo 方案保留为研究历史，不再参与当前运行时实现。

## 播放器层级动画、作者身份与公开发现

2026-08-19 完成本轮三级播放器与访客发现路径验收。二级控制条重构为封面/曲目信息、进度与传输控制三个分区；一级→二级、二级→三级、三级→二级、二级→一级分别使用专属动画，并由相邻层级状态机约束，不允许跨级返回。拖拽入口对无活动指针的合成事件安全降级，避免因捕获指针失败阻断操作。

| 验收区域 | 桌面端（1280×720） | 手机端（375×812） | 结果 |
|---|---|---|---|
| 二级播放控制与层级返回 | 三分区信息层级清楚；一级→二级、二级→三级、三级→二级、二级→一级的专属过渡均在运行态触发；三级只可回到二级 | 固定播放条位于右下安全边距，未遮挡首屏主动作或文章首屏；控件与进度区域保持可辨识 | 通过 |
| 首页作者身份卡 `/` | 作者头像、昵称、简介、关注方向、当前状态及 GitHub/Email 清晰独立于首屏引语呈现 | 身份卡在一言之后单列展示，状态与联系入口无横向溢出 | 通过 |
| 文章详情 `/posts/ddd` | 返回入口旁呈现搜索、系统分享与复制链接；文末呈现继续阅读和紧凑作者卡 | 三个分享操作在标题前保持同一触控行；封面、正文、继续阅读和作者卡按自然阅读顺序单列排列 | 通过 |
| RSS、sitemap、OG | `/rss.xml` 返回真实已发布文章 `ddd`；`/sitemap.xml` 列出公开栏目与该文章；文章页标题、description、canonical、`og:type=article`、OG 图片均由服务端注入 | 公开端点与页面元信息不依赖手机端脚本，适用于订阅器、搜索引擎和分享抓取 | 通过 |

运行态核对：文章页的 `og:title` 为 `ddd · MorroBlog`，canonical 指向当前文章的公开 URL；RSS 与 sitemap 均使用当前 Host 构造 URL。访客点击“复制链接”后，控件即时切换为“已复制”，确认剪贴板回退链路和视觉反馈均有效。绑定稳定域名时可设置可选的 `PUBLIC_SITE_URL`，详细说明见 `PUBLIC_DISCOVERY.md`。当前真实文章未添加标签，因此“继续阅读”诚实显示后续可由标签延伸的说明，不伪造关联内容。

质量验证：`pnpm check` 通过；Vitest **44 个测试文件、111 项通过，1 项显式 SMTP 集成测试跳过**；`pnpm build` 通过。新增 `publicDiscoveryContract.test.ts` 覆盖作者维护入口、首页/文末身份卡、真实标签关联、分享降级、RSS、sitemap、robots 与 OG 元信息结构。

## 首页身份、一言与开场叙事重排

2026-08-19 按最新信息层级调整首页：完整作者身份卡移到公开一言的左侧，原先首屏左栏的开场内容下移到第二个阅读区；桌面端两列分别承担“我是谁”与“此刻读到什么”，下方再以“为什么留下记录”与阅读入口承接内容，不再让不同任务争夺同一首屏。

同时收束重复说明。作者卡保留一次关于开源、AI 工具、系统与硬件的身份介绍、关注方向与维护状态；下方开场改为“折腾不是履历，是下一次少走弯路的底稿”，说明记录的目的、复盘范围与读者价值，不再重复技术领域清单。右侧“从这里继续”只负责文章、时间轴和标签的阅读路径。

| 视口 | 核验结果 |
|---|---|
| 1280×720 | 作者卡位于左列，一言位于右列；下方标题与行动入口独立出现，信息分工明确、没有内容重叠。 |
| 375×812 | 作者卡、一言、复盘开场、阅读入口和最新文章按单列依次展开；无横向溢出，标题、按钮与底部播放器不相互遮挡。 |

质量验证：`pnpm check` 通过；Vitest **44 个测试文件、111 项通过，1 项显式 SMTP 集成测试跳过**。首页与访客导航契约已同步保护作者卡在一言左侧、开场叙事下移和不再在首页直接重复作者介绍字段。

后续按版面收束要求，已移除该复盘开场区右侧的“从这里继续”卡片。现在“一言”下方只有横跨整行的“折腾不是履历，是下一次少走弯路的底稿”主叙事区，标题、说明、主题标签与两项阅读操作在同一条内容线中展开。1280×720 下该区域完整利用两列宽度；375×812 下保持单列，操作仍在正文后连续可达。`pnpm check` 与全量 Vitest 再次通过（**44 个文件、111 项通过，1 项显式 SMTP 集成测试跳过**）。

## Halo 适配统一管理中心

2026-08-19 按用户授权直接引入 Halo Console 固定提交 `d6616cf7031f6113cfb5c317dc88abd9e674c44e` 的原始 `BasicLayout.vue` 与完整 GPL-3.0 许可证，并以 React/Wouter/tRPC 适配为独立 Console Shell。原始文件、许可证、来源、固定提交与适配说明分别保留在 `third_party/halo-console/` 和 `THIRD_PARTY_CONSOLE_ATTRIBUTION.md`；项目许可证已同步为 `GPL-3.0-or-later`。

| 验收区域 | 桌面端运行态 | 375×812 手机端 | 结果 |
|---|---|---|---|
| 管理员认证 | 新建本地邮箱管理员账号成功登录，公开首页真实显示“已登录 / 管理员视角 / 退出” | 独立截图环境无真实 Cookie，正确仅显示管理员权限回退 | 通过 |
| 独立 Console Shell `/admin` | 访客顶部导航不再出现；左侧显示 Halo 结构的分组侧栏、账户区、主内容区与“查看站点”返回路径 | 无会话访问不渲染受限控件；移动布局提供抽屉菜单入口 | 通过 |
| 既有内容工作流 | 概览统计与真实数据一致（已发布 1、草稿 0、待审核评论 0）；文章、标签与分类、图片集、评论、用户入口均由现有 tRPC 数据驱动 | 未伪造文章、评论、图片或通知 | 通过 |
| 控制台写作 | 侧栏“写文章”进入 `/admin/content/new`，原有全屏编辑器加载并显示“返回管理中心”；未保存任何测试文章 | 编辑器继续使用原有响应式写作布局和未保存提示 | 通过 |

管理员验收账号已通过站内 `createLocalUser`、bcrypt 哈希和邮箱已验证模型创建，并由数据库核对为 `role=admin`；密码未进入源码、文档或数据库查询输出。一次性初始化脚本完成后已删除，账号保留但项目不留下可重复运行的预置入口。质量验证：`pnpm check` 通过；Vitest **45 个测试文件、114 项通过，1 项显式 SMTP 集成测试跳过**；`pnpm build` 通过。生产构建对主入口的大体积包仍提示优化建议，但未产生构建错误。

## Halo 内容编辑与图片集 5MB 边界

2026-08-19 继续直接适配 Halo Console 固定提交 `d6616cf7031f6113cfb5c317dc88abd9e674c44e` 的 `PostEditor.vue`、`DefaultEditor.vue` 与 `AttachmentList.vue`。上游原始文件保留在 `third_party/halo-console/`，新增的来源与 React/Tiptap/tRPC/Markdown 差异已补充到 `THIRD_PARTY_CONSOLE_ATTRIBUTION.md`。MorroBlog 的编辑数据仍以 Markdown 持久化；为降低编辑真实文章时表格、删除线和任务列表被转换丢失的风险，Markdown 往返层使用 GFM 规则。

| 验收区域 | 管理员运行态 | 结果 |
|---|---|---|
| Halo 文章编辑 | `/admin/content/new` 实际显示保存/设置/发布操作、撤销/重做、三级标题、加粗、斜体、下划线、删除线、引用、代码块、列表、任务列表、对齐、表格与附件入口；“大纲/详情”可切换，详情实时显示 0 字、0 词和 Markdown 格式 | 通过 |
| 图片集单文件限制 | 客户端上传控件、当前图片集说明和服务端二进制校验统一为**不得超过 5MB**；服务端回归使用 `5MB + 1 byte` 负载确认拒绝且不写入存储 | 通过 |
| Halo 附件库式媒体管理 | 真实图片集“呃”在不写入数据的情况下被选中；目录、真实 0 张状态、过滤输入、网格/列表切换、刷新与危险操作提示均已渲染；未选择图片、未修改元数据或删除记录 | 通过 |

质量验证：`pnpm check` 通过；Vitest **46 个测试文件、117 项通过，1 项显式 SMTP 集成测试跳过**；`pnpm build` 通过。编辑器依赖使惰性加载的 `CreatePost` 页面块增大，但不进入首页主入口；构建仅保留既有的大块提示，不产生构建错误。

## 标签主题索引重构

2026-08-19 先完成公开实现调研并记录于 `TAG_INDEX_RESEARCH.md`：Astro 的标签教程强调从真实内容提取唯一标签并链接至可恢复的标签页；PaperMod 将分类法、搜索和响应式浏览组织为连续阅读路径。本轮只吸收这些信息架构原则，不复制其主题、模板或视觉代码。[1] [2]

| 视口 | 验收结果 |
|---|---|
| 1280×720 | 原本的单行标签清单已替换为“主题索引”首屏、真实主题/公开关联计数、目录说明、非对称编号主题卡与“全部文章”回退路径。当前仅有的真实标签“开源”诚实显示 0 篇公开文章。 |
| 375×812 | 首屏的主题叙事、真实计数、主题卡和全部文章入口按单列展开；序号、主题名、状态和方向信号同屏可读，无横向溢出。 |

每张主题卡均为完整可点击链接，指向既有 `/posts?tag=` 筛选 URL；卡片没有虚构“热门”“趋势”或不存在的关联内容。新增 `tagIndexExperienceContract.test.ts` 守护真实计数、筛选路径、焦点状态和准确空状态。

质量验证：`pnpm check` 通过；Vitest **47 个测试文件、120 项通过，1 项显式 SMTP 集成测试跳过**；`pnpm build` 通过。

## References

[1] [Astro — Build a tag index page](https://docs.astro.build/en/tutorial/5-astro-api/3/)

[2] [Hugo PaperMod — Features](https://github.com/adityatelange/hugo-PaperMod/wiki/Features)

## Halo 同款管理中心与 30MB 图片上传

2026-08-19 按用户授权继续直接适配 Halo Console 固定提交 `d6616cf7031f6113cfb5c317dc88abd9e674c44e` 的 `Dashboard.vue` 与 `PostList.vue`，并将未修改上游副本保留在 `third_party/halo-console/`。新内容已登记于 `THIRD_PARTY_CONSOLE_ATTRIBUTION.md`，项目继续以 GPL-3.0-or-later 分发。

| 验收区域 | 实际结果 |
|---|---|
| Halo 同款控制台构图 | 真实管理员会话下，`/admin` 使用独立白色侧栏、分组菜单、固定页头、仪表盘统计组件、最近内容、快捷入口和页面级内容工作区；文章库沿用同款页头/动作区/实体列表节奏。访客导航未在控制台出现。 |
| 真实管理功能 | 仪表盘显示实际统计：已发布文章 1、草稿 0、待审核评论 0；文章 `ddd` 可继续进入编辑、公开查看或删除确认。图片集当前真实数据为 0 个，控制台诚实显示目录与选择前空状态，不伪造媒体。 |
| 30MB 上传限制 | `ImageUpload`、Halo 富文本编辑器、图片集上传入口和服务端 `media.uploadImage` 统一为 30MB。Base64 请求上限同步为 42,000,000 字符，可容纳 30MB 原始文件；`30MB + 1 byte` 回归负载由服务端以 `PAYLOAD_TOO_LARGE` 拒绝。 |

质量验证：`pnpm check` 通过；Vitest **47 个测试文件、121 项通过，1 项显式 SMTP 集成测试跳过**；`pnpm build` 通过。构建仍仅提示既有的入口/编辑器大块优化建议，没有构建错误。

## 评论发表与审核治理优化

2026-08-19 在不创建评论数据的前提下完成评论体验升级。评论内容由客户端提示和服务端 Zod 同步限制为 2,000 字符；新提交始终进入待审核状态；文章页将待审核评论明确标注为“仅你可见 · 待审核”，并为回复打开后自动聚焦、字数反馈、成功/失败状态提示和登录回退补齐可访问语义。

| 验收路径 | 真实运行结果 |
|---|---|
| 管理员 `/admin` → 评论 | 已登录管理员可进入“互动治理 → 评论审核”，使用全部、待审核、已通过、已拒绝四种状态筛选及刷新操作。当前真实数据为 0 条，准确显示“没有等待处理的评论”，不伪造审核记录。通过、拒绝、退回审核与重新审核均使用受 `adminProcedure` 保护的真实状态写入接口。 |
| 管理员文章页 `/posts/ddd` | 评论区显示 0 条真实评论、已登录发表框、`0/2000` 字数反馈、审核可见性说明和提交控件。未输入或提交任何测试评论。 |
| 访客手机 375×812 | 未登录访客不见发表框，显示“登录后才能发表或回复评论”的明确回退；0 评论空状态、阅读区和底部播放器不重叠，无横向溢出。 |

质量验证：`pnpm check` 通过；Vitest **48 个测试文件、124 项通过，1 项显式 SMTP 集成测试跳过**。新增 `commentExperienceContract.test.ts` 守护同文长度、父评论归属、真实状态筛选、审批回转、回复焦点、待审核可见性和登录路径。
