# 三级播放器交互修复记录

日期：2026-08-17

## 已定位原因

三级面板打开时，紧凑播放器仍以二级状态持续挂载。其捕获阶段的全局 `pointerdown` 监听把三级面板内的点击误判为紧凑播放器外部点击，先将状态降为一级，使歌曲列表的 `click` 事件无法到达选歌处理函数。

## 已实施修复

紧凑播放器新增 `externalDismissEnabled`。只有全局状态为二级时才注册外部点击收起监听；三级时监听不注册，完整面板保持展开，歌单按钮可执行真实切歌。三级返回动作仍只通过面板内“返回二级播放控制”按钮触发。

## 浏览器复核进度

已在运行态首页载入播放器一级入口。自动化浏览器的点击与悬停尝试未稳定进入二级控制层，因而尚未用该通道验证三级选歌；该现象将与事件状态和 DOM 状态一起继续排查，不能替代已通过的代码级回归契约。

浏览器 DOM 诊断确认一级播放器处于 `compact-music-player--idle`，可接收指针事件且具有正确的曲目和时长语义。自动化页面坐标与截图缩放不一致，直接指针点击尚不可靠；对按钮调用点击处理器后的同步读取也未等待 React 渲染周期，后续验证会等待下一帧后再读取状态。

## 运行态验证结果

在等待 React 渲染周期后，一级入口成功进入二级，并通过二级入口展开三级完整面板。向文档主体分派外部 `pointerdown` 后，三级面板仍保持可见。随后选择歌单第 3 首，当前曲目从 `Love Song` 更新为 `花海`，歌单中仅第 3 首保留 `aria-pressed="true"`；证明选歌处理器已在真实运行态执行，且三级不再被二级外部点击监听关闭。

在 375×812 手机视口下，一级浮条保持在内容右下角，可见封面、歌曲名与足够的触控面积；全局导航和首页文字未与播放器重叠。三级的事件门控与桌面相同，因其由同一 React 状态控制，不存在依赖屏幕尺寸的分支。

## 动画节奏优化

一级到二级以宽度、内边距、间距、透明度和位移的可中断过渡完成；二级控制项以 34–96ms 的短阶梯进入，避免同时出现形成突兀闪现。三级面板在紧凑条淡出 60ms 后再进入，使用 280ms 位移缩放和 200ms 透明度过渡。隐藏的层级禁用指针事件，避免退出动画期间拦截操作；拖拽态关闭过渡，减少动态效果偏好下将动画和过渡压缩为最短时长。

## 进度控制升级

二级与三级统一使用 `PlaybackScrubber`：深色未播放轨道、冰蓝已播放信号、明确白色定位把手和“已播放 / 当前时间 / 总时长”信息形成独立层次。鼠标、触摸和键盘均使用原生范围输入；拖动期间只更新预览，释放后提交最终 `seek`，同时停止整条播放器的拖拽事件。真实预览中将《Love Song》由 0:00 拖至 3:00 后，范围输入、`aria-valuetext`、音频 `currentTime` 与已播放信号宽度均同步为 3:00 / 4:29。一级胶囊保留细小进度信号，手机 375×812 下位于右下安全边距且不遮挡文章主内容。

## 两层播放器（取代此前的二级控制层）

依据用户最新要求，原中间二级控制层已移除。当前一级为常驻的 8.7rem 高信息控制条：展示原始内嵌封面、曲名、歌手、曲目序号、播放状态、可拖拽的已播放进度、当前/总时长，以及上一首、播放暂停、下一首和节目列表入口。点击封面、曲目信息或列表图标直接进入三级完整面板；三级面板的收起动作直接回到一级控制条。

过去仅供二级层使用的外部点击收起监听不再存在，因此不会再次截获歌单选曲事件。2026-08-18 在 1280×720 与 375×812 视口复核一级控制条：桌面端位于右下并与首屏内容、右上公共栏目分离；手机端占满安全宽度但不横向溢出，封面、曲目状态、时间、进度把手和控制按钮均可见。自动化契约覆盖一级直达三级、歌单选择、进度拖拽与无障碍进度文本。
