# MorroBlog 统一管理中心实施基线

> **结论：** 管理中心应先以 MorroBlog 现有 React、tRPC、Drizzle 和管理员权限体系重构信息架构；Halo 可以作为产品组织方式和公开源码研究对象，但当前不直接把 Halo Console 源码混入项目。

## 评审结论

用户提供的研究方案正确识别了当前的核心问题：内容能力已经存在，但入口分散于 `/admin`、`/workspace`、`/create` 与 `/edit/:id`。当前管理员后台已具备文章、待审核评论、标签与分类、图片集、用户五类操作；写作工作台已有草稿恢复、快捷保存、文档设置与发布流，但这些能力尚未由统一控制台承载。当前路由仍将 `/admin` 包裹在公共 `BlogLayout` 中，因此后台沿用访客导航，而不是独立的管理工作空间。

| 领域 | 现有可复用基础 | 首期缺口 | 第一阶段处理方式 |
|---|---|---|---|
| 概览 | 文章总览与待审核评论查询 | 最近编辑、快速动作、首页状态汇总 | 在控制台 Shell 中聚合现有查询，不新增虚构统计。 |
| 内容 | 文章列表、编辑、草稿、发布、标签、分类 | 全局文章库页面和稳定的后台路径 | 将既有列表与编辑流重组为控制台二级页面，不重写编辑器。 |
| 媒体 | 图片集、S3 上传、图片排序 | 跨图片集复用与引用关系 | 首期保留图片集；媒体库单独列入后续数据模型。 |
| 互动 | 待审核评论、通过/拒绝 | 全状态筛选、关联文章、管理员回复、批量处理 | 首期迁入“互动 → 评论”，不伪造评论数据。 |
| 站点 | `siteBrand.ts` 等代码级配置 | 持久化首页与作者配置、导航排序、外观白名单 | 第二阶段新增受管理员保护的数据模型与预览保存流。 |
| 系统 | 管理员角色、用户列表 | 操作审计、健康信息、角色管理 | 后续新增审计日志；角色与敏感操作不在首期改写。 |

当前数据库包含用户、验证码、文章、分类、标签、文章标签、评论、图片集与图片等实体；**没有**站点设置、首页模块、导航项、媒体库引用、回收站或审计日志模型。现有管理员 tRPC 接口同样集中于文章、用户和待审核评论，站点配置与系统治理尚无后端域。因此首期应避免为了“统一后台”而抢先迁移数据库：先完成壳层、路由、菜单与既有能力聚合，再按设置域新增表与迁移。

## Halo 源码复用边界

Halo 官方主仓库和归档的 Console 仓库都标注为 **GPL-3.0**；旧 Console 页面同时说明该管理端已经迁入 Halo 主仓库。[1] [2] MorroBlog 当前 `package.json` 声明为 MIT，且前端技术栈为 React 19，而 Halo Console 为 Vue 管理端。两者在许可证与运行时框架上均不具备“复制一个组件即可接入”的条件。

| 候选做法 | 是否采用 | 原因与要求 |
|---|---|---|
| 借鉴业务分组、左侧稳定导航、列表—详情—编辑路径与权限可见性 | **采用** | 这些是产品信息架构原则，不复制受版权保护的表达。 |
| 参考 Halo 文档与公开接口形态，重新实现 MorroBlog 的 React 页面 | **采用** | 保持现有 tRPC、数据库与身份体系，减少框架迁移与部署风险。 |
| 直接复制 Halo Console 的 Vue 组件、CSS、图标、文本或构建代码 | **暂不采用** | GPL-3.0 和 MIT 许可策略不匹配，且 Vue 组件无法直接挂入 React。若日后决定采用，必须先进行许可证与发布模式评审，保留版权、许可证、修改说明和对应源码；本文件不构成法律意见。 |
| 以 iframe 或独立服务运行 Halo Console | **不采用** | 它依赖 Halo 后端对象与 API，不能管理 MorroBlog 的 tRPC/Drizzle 数据，也会造成双鉴权和双数据源。 |
| 直接复用本项目已有的 Radix/Shadcn 基础组件并建立 Console Shell | **采用** | 项目已经具备 React 兼容基础组件，能以较低风险实现可访问的侧栏、抽屉与列表结构。 |

因此，用户许可“可部分使用 Halo 源码”会被保留为**受限选项**：只有某个候选文件的精确来源、SPDX/版权头、依赖、改动范围和全项目许可证影响都经过单独核验后，才可在独立变更中考虑。当前第一、二阶段不需要也不应直接拷贝 Halo Console 源码。

## 分阶段实施基线

### 第一阶段：统一入口与既有内容工作流

建立 `ConsoleShell`，由服务器已存在的管理员权限边界控制。桌面端使用可收起左侧栏，移动端使用抽屉菜单；管理员页面不再承载公共博客导航。建议将 `/admin` 重定向或过渡到 `/admin/overview`，并逐步形成下列页面：

| 导航分组 | 路径 | 复用来源 | 数据库迁移 |
|---|---|---|---|
| 概览 | `/admin/overview` | 当前文章/评论概览查询 | 否 |
| 内容 → 文章库 | `/admin/content/posts` | `admin.posts.list`、现有编辑动作 | 否 |
| 内容 → 写文章 | `/admin/content/new`、`/admin/content/:id/edit` | `CreatePost` 全屏编辑器 | 否 |
| 内容 → 标签/分类 | `/admin/content/tags`、`/admin/content/categories` | 现有术语查询/创建能力 | 否 |
| 媒体 → 图片集 | `/admin/media/galleries` | 现有图片集和上传组件 | 否 |
| 互动 → 评论 | `/admin/engagement/comments` | 现有待审核查询/动作 | 否 |
| 系统 → 用户 | `/admin/system/users` | 现有用户目录 | 否 |

首期必须保留 `adminProcedure` 的服务端鉴权；前端菜单隐藏仅用于体验，不可替代后端权限。现有普通作者工作区在第一阶段可继续保留，避免把普通作者错误提升到管理员控制台。

### 第二阶段：首页与站点设置

新增受 `adminProcedure` 保护的持久化站点设置域。推荐使用**受约束的领域模型**，而不是可任意拼装的页面搭建器：一条站点身份/作者配置、一组有白名单校验的背景配置、一条一言策略、一条首页精选策略，以及独立的导航项排序表。首页和关于页改为读取该配置，并保留 `siteBrand.ts` 作为未配置或读取失败时的稳定回退。每次保存应验证 URL、图片类型、文章归属和排序范围，支持预览与失败回退。

### 第三阶段：内容生命周期与媒体库

在真实需求明确后增加置顶、定时发布、评论开关、软删除/回收站与恢复；再抽取可跨文章、作者头像、背景和图片集复用的媒体库实体，并显示引用关系。所有删除前先识别引用，避免误删正在首页或文章中使用的资源。

### 第四阶段：互动与系统治理

扩展全部评论状态筛选、管理员回复、隐藏/恢复、通知偏好、操作审计与站点健康信息。任何角色改动、发布、删除、恢复、首页配置与背景白名单修改都应记录操作人、时间、对象和变更摘要。受用户此前指令影响，安全审计仍然暂停，不能将该阶段当作恢复安全测试的授权。

## 实施验收门槛

| 门槛 | 必须满足的条件 |
|---|---|
| 权限 | 公开路由、普通作者工作区与管理员 Console 路由均由服务端授权验证覆盖。 |
| 数据 | 不创建测试文章、评论、用户、图片或首页内容；真实数据不足时展示明确空状态。 |
| 兼容 | 现有 `/create`、`/edit/:id`、`/workspace` 与 `/admin` 迁移时保留可恢复路径，避免链接失效。 |
| 响应式 | 桌面侧栏与手机抽屉各自验证，不能仅缩放桌面后台。 |
| 质量 | 每个迁移模块新增或更新 Vitest 契约，类型检查、全量测试、构建和管理员/访客回退路径均通过。 |
| 许可证 | 若任何 Halo 源文件被复制、改写或分发，变更必须包含来源 URL、固定提交、许可证文本、版权保留、修改说明与单独的许可评估记录。 |

## 本轮结论

本轮仅完成研究与实施基线，不改写任何业务界面、路由、数据库或权限。可以立即开始第一阶段的 Console Shell 设计与现有能力迁移；第二阶段之前必须先完成设置模型、迁移顺序和配置回退设计。直接复制 Halo Console 源码当前不进入实现队列。

## References

[1] [Halo Console archive — repository status and GPL-3.0 declaration](https://github.com/halo-dev/console)

[2] [Halo main repository — GPL-3.0 declaration](https://github.com/halo-dev/halo)

[3] [GNU GPL v3 — copy, modification and conveying terms](https://www.gnu.org/licenses/gpl-3.0.html)
