# 管理中心运行验收记录

## 管理员账号

已通过站内 `createLocalUser` 路径创建并查询验证本地邮箱管理员账号：`console-admin-20260819@morroblog.local`。数据库返回其 `role=admin`、`loginMethod=email-password` 且邮箱已验证；密码仅在创建时生成，未写入项目文件、源码、数据库查询记录或验收文档。

创建完成后，一次性账号初始化脚本已从项目中删除；保留账号不会受到影响，也不会留下可重复运行的管理员预置入口。

## 浏览器验证进度

已在站内 `/login` 页面填写该账号并提交登录；提交后按钮进入“正在处理…”状态。下一步将确认会话创建、`/admin` 的服务端角色授权与控制台桌面/移动布局。

登录请求已完成并返回公开首页：全局导航显示“已登录”“管理员视角”“退出”，确认本地邮箱会话和管理员角色已在浏览器中生效。随后打开 `/admin`，初始按懒加载契约显示“正在打开页面… 内容马上就好。”，等待控制台模块渲染后继续验证。

`/admin` 已完成加载：页面只显示独立管理中心的侧栏、账户区、概览、文章库、写文章、标签与分类、图片集、评论、用户与角色入口，不显示公开博客导航；概览数据与数据库一致（已发布 1、草稿 0、待审核评论 0）。从侧栏“写文章”进入 `/admin/content/new` 后，原有全屏编辑器正确加载并将返回操作显示为“返回管理中心”。为遵循不伪造文章内容的约束，本次未填写或保存任何测试文章。

2026-08-19 在同一管理员会话重新打开 `/admin/content/new`，Halo 适配编辑器完成实际挂载：返回管理中心、设置、保存草稿、发布文章、标题输入、撤销/重做、三级标题、加粗/斜体/下划线/删除线、引用、代码块、列表、任务列表、文本对齐、表格和“附件”上传入口均可见且可达；编辑主体具有“文章正文”无障碍名称，右侧可切换“大纲/详情”，空文稿诚实显示“暂无大纲”。本次没有输入、保存或发布测试文字，也未选择图片文件。

依赖重启后再次访问 `/admin/content/new`，编辑器与全部工具栏、附件入口和大纲侧栏仍正常载入。点击“返回管理中心”后，控制台保持独立 Halo 适配布局：左侧分组导航（工作台、文章库、写文章、标签与分类、图片集、评论、用户与角色）、账户区、查看站点出口和概览统计均可见，未出现访客导航。当前真实统计仍为已发布 1、草稿 0、待审核评论 0。

图片集入口运行态验证：控制台侧栏可进入“图片集”；当前真实数据为 1 个名为“呃”的图片集。在尚未选择图片集时，页面只显示目录、新建表单和明确的“先选择一个图片集”说明，不渲染上传、筛选或移除控件，避免对未指定集合执行操作。自动化契约已覆盖选择后才显示 5MB 上传限制与 Halo 附件库式筛选/视图/刷新控制；对现有集合的选择动作将继续以不写入数据的方式复核。

为排除浏览器标注点击偏差，页面 DOM 精确定位到文本为“01呃”的目录按钮；该按钮 `disabled=false`，其真实点击处理器已被触发。控制台未记录运行时错误，后续以页面稳定态确认查询与选中渲染结果。

稳定态确认当前图片集“呃”已被选中，真实数据为 0 张图片。页面显示其真实说明“问问”、可公开浏览状态、上传前确认、`单文件不得超过 30MB。浏览器与服务端都会拒绝超限文件。`、上传控件的 `最大 30MB` 提示，以及“过滤当前图片集”、网格模式、列表模式、刷新图片集三个 Halo 附件库式控制。未选择任何文件、未上传图片、未修改图片集元数据或执行删除操作。

2026-08-19 在同一真实管理员会话复核 Halo 同款仪表盘结构：独立白色分组侧栏、固定页头、页面标题/说明/刷新动作、四项统计组件、最近内容卡片和快捷入口均完成渲染。所有统计与路径来自真实数据：已发布文章 1 篇（`ddd`）、草稿 0、待审核评论 0；点击统计和快捷入口均使用现有控制台内部路由，不创建任何内容记录。此版本继续保留“查看站点”、账号区和退出入口。

2026-08-19 在同一管理员会话打开真实文章 `/posts/ddd`。评论区正确显示 0 条真实评论与不伪造数据的空状态；已登录表单包含“留下你的看法”、审核可见性说明、`0/2000` 字符计数和提交评论动作。由于当前数据库没有评论，为遵循不伪造用户内容的边界，本次未填写、提交、审批或删除评论；管理员全状态筛选、通过/拒绝/退回审核的前端与服务端权限仍由新增自动化契约覆盖。

评论功能优化后重新打开 `/admin`，Halo 同款仪表盘稳定显示真实统计：已发布文章 1、草稿 0、待审核评论 0，最近内容准确列出 `ddd` 为已发布。页面提供“处理评论”入口，接下来的运行态检查只浏览零评论数据下的筛选、刷新和空状态，不创建评论。
