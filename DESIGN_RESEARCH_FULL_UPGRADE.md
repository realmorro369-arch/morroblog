# 全站升级交互研究记录

日期：2026-08-17

本记录仅提炼公开站点和开源项目的产品原则，不复制其品牌、文案、版式、图像、源码或视觉成品。

## blog.morro.asia：内容时间与访问线索

用户指定站点将“首页、时间轴、标签、友链”作为明确且重复可达的主要入口；文章卡片显示发布日期、封面、标题与摘要；侧栏将最新文章、标签、月度归档与站点信息组织为可返回的浏览线索。MorroBlog 将只吸收以下原则：以真实发布日期组织公开文章时间线、让同一内容存在列表/标签/归档多条查找路径、在移动端保留低密度的内容导航。不会使用其 Butterfly 主题视觉、蓝色首屏、侧栏样式、文案或图片。

## Ghost：发布与管理边界

Ghost 官方仓库展示了面向现代发布、会员和订阅的成熟发布平台，并以管理端、发布核心与编辑器相关模块分离维护。MorroBlog 将吸收“将内容状态、编辑体验与发布反馈作为独立关注点”的原则：在管理台清楚区分内容状态和可执行操作，在工作台提供连续文稿与可恢复编辑流，并将每项后台动作保留明确反馈。不会复制 Ghost 的管理台视觉、编辑器代码、品牌资产或订阅产品机制。

## Outline：清晰内容组织与操作反馈

Outline 官方仓库将自己定位为快速、协作、功能完整且兼容 Markdown 的知识库。其公开开发记录强调背景操作的反馈、可恢复内容层级和稳定附件 URL 以减少重复请求。MorroBlog 将吸收“操作完成后给出局部、可恢复的反馈”和“内容资源保持可缓存、避免重复获取”的原则：管理台操作将反馈当前处理对象和完成状态；写作/管理界面的派生列表将使用稳定计算；图像、音频与异步任务会避免在不需要时反复初始化。不会引入协作知识库、导入管线、品牌或视觉样式。

## Tiptap：组件生命周期与增量更新

Tiptap 官方仓库是可组合的无头编辑器框架，其公开开发记录持续关注装饰组件在卸载时释放、增量更新而非整份重建、稳定键与缓存资源的边界。MorroBlog 将吸收“编辑器与浮层组件必须清理事件/资源、派生数据使用稳定记忆化、非关键功能按需加载”的原则。现有 Markdown 写作模型将继续保留，不会切换编辑器框架或复制 Tiptap 的扩展及界面。

## 初步转化方向

本轮将采用本项目既有的夜蓝、暖白、冰蓝与低饱和珊瑚色设计令牌。时间线、写作工作台、管理面板和过渡动画将共享这套语言，并以真实文章、真实角色和现有权限为数据边界；无真实内容时只展示准确空状态。

## 访客首页升级：作者先于功能，内容紧随其后

本轮再次阅读 `blog.morro.asia` 首页。其可迁移价值不在于 Butterfly 的蓝色视觉或侧栏，而在于它同时提供“博主身份、短说明、内容数量、导航、最新文章、时间轴和标签”几条明确的访客线索。MorroBlog 将改为在首屏左侧先说明 Morro 关注的技术方向和本站记录范围；右侧以公开一言作为轻量的即时停留点；首屏之后立即呈现当前真实最新文章，使第一次访问者能先理解“这里是谁在写、写什么”，再选择继续阅读。不会复制该站的头像构图、计数、社交入口、蓝色大图、主题样式、公告或任何原始文案。

同时参考 [Lee Robinson 首页](https://leerob.com/) 的信息组织：先用简短个人背景建立作者可信度，再把长期关注主题作为可扫描线索，最后让按日期排列的真实文章承担主要继续阅读动作。MorroBlog 只会采用“身份说明 → 关注方向 → 最新文章”的顺序与低噪音文章列表原则；保持自身夜蓝刊物排版、中文文案、背景图与 `v1.hitokoto.cn` 的公开一言，不复制其姓名、履历、主题词、文章或版式。

## Halo 播放器生态：研究取材与职责分层

本节研究对象是 Halo 官方应用市场所列的 **Navidrome 播放器** 与 **APlayer** 插件，以及 Navidrome 播放器公开仓库；该结论只分析已公开的功能说明与代码结构，不执行或复用第三方代码。Halo 官方页面将 Navidrome 播放器定义为面向前台的全局音乐能力，覆盖悬浮播放、歌单切换、搜索、连续听歌、播放位置与音量持久化，以及为不同主题兼容性准备的 PJAX 策略。[1] 其公开仓库亦明确描述了“悬浮控制 + 可展开面板 + 多歌单切换 + 前端播放状态持久化”的产品定位。[2]

相对地，Halo APlayer 插件的职责是将单曲或 Meting 组件嵌入文章编辑内容中，而不是承担全站持续播放；其公开使用说明要求作者在文章内插入 APlayer 或 Meting 标记，并可配置外部 Meting API。[3] 因而 MorroBlog 现有全局播放器应继续保持全站阅读陪伴这一单一职责，不应为了模仿 Halo 而把文章内嵌播放器、外部聚合音乐源或 CMS 主题兼容层混入当前音乐控制路径。

| 研究对象 | 已核验的原始定位 | 对 MorroBlog 的判断 |
| --- | --- | --- |
| Halo Navidrome 播放器 | 基于 Navidrome / Subsonic API 的全局前台播放器，提供悬浮控制、歌单与搜索等连续聆听能力。[1] | 可借鉴“随页面持续存在的播放器状态机”与“最小控制层 + 深层浏览层”的信息架构；继续使用用户授权的既有曲目，不新增外部音源依赖。 |
| Halo APlayer 插件 | 文章内插入式播放组件，可由 APlayer 或 Meting 标记生成。[3] | 不作为当前全局播放器的改造对象；只记录其“内容内媒体与全站媒体应分责”的边界。 |

### 来源

[1]: https://www.halo.run/store/apps/app-njgda530/releases/app-release-4n7eatot "Halo 应用市场：Navidrome Player for Halo v4.7.3"
[2]: https://github.com/queenyn/halo-plugin-navidrome-player "queenyn/halo-plugin-navidrome-player"
[3]: https://www.halo.run/store/apps/app-stWoc "Halo 应用市场：APlayer"

### 深度实现观察：行为比外观更值得迁移

> Halo Navidrome 插件将悬浮入口、主播放面板、歌单抽屉和搜索抽屉拆为彼此独立的状态；切换抽屉时只改变对应局部，不销毁已挂载的播放实例。该策略能让“继续听歌”与“继续浏览内容”互不干扰。[2]

对其公开 `navidrome.js` 与项目截图的阅读表明，成熟度主要来自状态边界而非覆盖性的装饰。代码将全局播放状态挂到一个幂等启动对象上，反复挂载时复用音频实例；采用约 1 秒的延迟写入保存播放位置、暂停状态、音量、当前歌单和悬浮位置，以避免随 `timeupdate` 高频同步存储。[2] 当媒体元数据尚未可用时，它先更新可见进度与时间预览，再在 `loadedmetadata` 或 `canplay` 事件中应用真正的跳转，避免首次加载或恢复时出现进度条回弹。[2]

其进度控件对鼠标与触控都以“开始预览 → 文档级跟随 → 松手提交”的交互链路处理；键盘只在播放器展开、焦点不在可编辑内容中时响应左右方向键，并用 `Shift` 扩大时间步长。[2] 歌词以解析后的时间戳为依据，通过 `requestAnimationFrame` 在播放时更新，暂停和换曲时主动停止更新循环；歌词结果按照曲目标识缓存，并在异步结果返回后再次确认仍属于当前曲目，避免旧请求覆盖新歌曲。[2]

从截图可见，Halo 的信息结构采用封面、曲名/歌手、两行歌词、进度和常用控制，随后再展示曲目列表；歌单与搜索被放置于与主面板并列或上方的抽屉中。这一布局解决的是“播放器始终可用但不抢走阅读主线”的问题；不过其浅色圆角卡片、APlayer 图标体系、宽行高曲目列表与大量并列控制均不适合直接进入 MorroBlog 的夜蓝刊物语境。

| 观察维度 | Halo 的可验证做法 | MorroBlog 当前状态 | 结论 |
| --- | --- | --- | --- |
| 播放进度 | 预览值与真实媒体时间分离；媒体可跳转后再提交恢复定位。[2] | `PlaybackScrubber` 已有拖拽预览与松手提交语义。 | **保留现有组件**，不重写；仅在首次恢复时补充媒体就绪前的待恢复状态。 |
| 持久化 | 保存当前歌单、曲目、进度、暂停、音量与浮层位置；播放过程中节流保存。[2] | 仅保存背景图片，不保存播放上下文。 | **优先补齐** 播放上下文与节流写入，并在会话恢复中尊重浏览器自动播放限制。 |
| 键盘 | 在非输入区、展开态时提供左右键 5 秒、`Shift`+方向键 15 秒跳转。[2] | 无全局音频快捷键。 | **可迁移**，但应新增空格键与输入/按钮焦点豁免规则，且仅在三级面板打开时生效。 |
| 面板空间 | 根据右侧/左侧可用宽度决定抽屉方向；窄屏移到上方；窗口变化时重新限位。[2] | 一级与三级各自限位，但没有统一的 resize 重算与位置持久化。 | **可迁移为较小版本**：恢复已保存的位置、窗口变化时统一限位；不引入复杂双抽屉。 |
| 列表与检索 | 可配置滚动或分页，支持当前歌单搜索与“定位当前曲目”。[2] | 固定 7 首授权曲目，完整曲目直接可见。 | **不采用搜索/分页**；曲目规模不需要额外认知负担。可考虑在三级增加“当前播放”定位标识。 |
| 跨页持续播放 | 为多主题 CMS 提供 off/plugin/theme-compatible 三种 PJAX 策略与 DOM 重挂载修复。[1] [2] | React 根布局持续挂载 `<audio>`，路由切换不销毁。 | **明确不采用** PJAX/MutationObserver；这会重复现有 SPA 生命周期并提高故障面。 |

因此，初始结论曾建议围绕“**记得你上次听到哪里，但不替你强行播放**”补齐状态韧性，并维持七曲直选而不引入抽屉。

## 用户指定的 Halo 同级结构实施

在完成上述研究后，用户明确要求播放器采用 **Halo Navidrome 同级的完整交互结构**，而非只迁移差异化子集。该要求覆盖悬浮入口、主播放面板、节目单抽屉、搜索抽屉、当前曲目定位、持续播放语义、音量、播放顺序、歌词与本地状态恢复；实现仍保持 React 根布局持续挂载和既有 `morro.asia` 七首已授权曲目，不引入 Navidrome、Subsonic、APlayer、PJAX 或第三方前端代码。[1] [2]

| Halo 结构能力 | MorroBlog 最终实现 | 数据与技术边界 |
| --- | --- | --- |
| 可拖拽悬浮入口 | 64px 真正曲目封面入口，显示曲目序号和播放/暂停状态，点击进入主面板 | 继续使用用户确认授权的内嵌封面，不使用 Halo 图标或素材。 |
| 主播放面板 | 封面、曲名/歌手、当前/下一句 LRC、进度拖拽、上一首、播放暂停、下一首和收起动作 | `PlaybackScrubber` 保持“预览后提交”的既有可访问控件语义。 |
| 节目单与搜索抽屉 | 同一主面板提供“节目单”“搜索”“定位”动作；搜索按歌名与歌手实时过滤，选曲后同步面板并关闭搜索抽屉 | 仅针对真实的七首曲目运行；没有虚构的多节目单、分页或远程库。 |
| 播放顺序与音量 | 顺序循环、单曲循环、随机播放三态切换，以及可访问的音量范围输入 | 结束事件针对不同播放模式执行对应真实行为。 |
| 状态韧性与跨页 | 保存曲目、进度、暂停状态、音量、模式与位置；约 1 秒节流；元数据就绪后恢复定位；路由切换保持同一音频元素 | 首次访问静默；不以保存状态绕过浏览器自动播放策略。 |
| 窄屏适配与键盘边界 | 宽屏抽屉从主面板左侧展开，窄屏位于主面板上方；窗口尺寸变化重新限位；非表单焦点下支持 5/15 秒方向键跳转 | 关闭抽屉使用 `inert`，不会让隐藏搜索或曲目控件进入键盘焦点序列。 |

这一调整遵循用户对于功能结构和交互密度的直接要求；视觉仍采用 MorroBlog 的夜蓝、冰蓝与暖白令牌，且不复制第三方源码、品牌、图像或外部服务接口。
