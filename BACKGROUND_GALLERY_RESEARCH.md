# 随机图库背景调研记录

调研地址：<https://blog.morro.asia/>。

## 已确认的资源入口

页面首屏的 `#page-header` 与页脚 `#footer` 都使用 `https://t.alcy.cc/ycy` 作为 CSS 背景图片地址。该地址看起来是随机图库端点，因此每次请求可能返回不同图片。文章卡片缩略图使用另一组静态资源地址 `https://tc.alcy.cc/tc/.../*.webp`，不应混入全站背景轮换逻辑。

## 对 MorroBlog 的适配建议

随机端点适合用于低频的氛围背景，不适合作为每次路由切换或频繁刷新时的主视觉来源。实际实施时应仅在首次进入或用户主动换图时请求一次，并在内存中保留结果；在图片加载完成前保持现有深色底色。背景上应有半透明深色蒙层，确保标题、摘要和导航达到稳定对比度；移动端应使用较浅的遮罩或单独裁切策略，避免人像或主体被标题覆盖。还应在 `prefers-reduced-motion` 下禁用背景淡入切换，并保留随机图库失败时的纯色背景回退。

## 当前样本的风险

随机端点实际返回了一个 `4096 × 2302` 的 WebP 图片，并重定向到 `tc.alcy.cc` 的具体文件。该分辨率远高于首页背景在大多数设备上的显示尺寸，若直接作为全尺寸 CSS 背景请求，可能带来不必要的首屏传输开销。该样本同时包含不适合通用博客背景的成人向/性感二次元形象；因此不建议将未筛选的随机端点直接接入公开首页。若要使用这一图库，应先建立允许清单或改用经人工筛选的图片集合，再按照首屏预加载、深色遮罩和失败回退策略接入。

## 白名单复核

对博客现有文章图片候选进行复核后，随机图库中的二次元图片也可能包含明显的人体、性感姿态或视觉干扰元素，因此不能仅凭“已出现在博客文章中”就纳入全站背景。背景白名单应优先使用当前站点已有的天文、城市、自然、建筑和抽象图；二次元图片只有在人工确认构图、尺度和内容适合后才可加入。

继续复核发现，博客文章封面候选大多是高分辨率二次元人物图（约 4000–7680 像素宽），即使画面本身没有明显成人内容，也会因人物主体、亮色背景和构图焦点导致标题叠加困难。因此当前实现应使用已确认安全且低干扰的站内天文图作为首个白名单资源；随机图库来源只作为后续人工筛选素材池，不直接在客户端随机请求。

## 头像标识参考

参考地址：https://blog.morro.asia/

参考站点的头像资源为 `/img/avatar.png`，页面左上角使用头像/站名作为品牌入口；站名为 MorroBlog，副标题为“记录技术”。当前 MorroBlog 项目左上角仍是方形 M 字母标识，因此后续可将其替换为头像图片，同时保留站名、副标题和返回首页的可点击行为。
