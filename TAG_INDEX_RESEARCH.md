# 标签页主题索引研究

## 研究范围与结论

本轮调研聚焦开源博客与文档系统如何组织“标签 → 可继续阅读”的索引，而不是复制第三方页面视觉。MorroBlog 当前真实数据中标签数量有限，改版应优先建立**可扫描的层级、每个标签的明确去向、准确计数和空状态**；不应为了制造热闹感添加虚构主题、趋势、热度或文章数量。

| 来源 | 可迁移的事实与原则 | MorroBlog 采用方式 | 不采用的内容 |
|---|---|---|---|
| [Astro：Build a tag index page](https://docs.astro.build/en/tutorial/5-astro-api/3/) | 标签索引应从所有文章提取唯一标签，并将每个标签链接至其专属归档页面；标签集合不能在页面内手工硬编码。 | 继续使用现有真实 `trpc.tags.list` 数据和 `/posts?tag=` 路径；每个视觉单元均是可操作的筛选链接。 | Astro 的文件路由、示例标签名称与样式代码。 |
| [Hugo PaperMod](https://github.com/adityatelange/hugo-PaperMod) | 高质量技术主题强调响应式、分类法（taxonomies）、搜索与可恢复的阅读路径。 | 采用“主题索引 → 继续阅读”的明确入口、低依赖的布局和移动端单列浏览。 | PaperMod 的模板、视觉、代码与品牌。 |
| [PaperMod Features](https://github.com/adityatelange/hugo-PaperMod/wiki/Features) | 主题系统可将内容分类、搜索、阅读时长、面包屑等组合为连续的浏览体验。 | 标签页只承担主题发现；筛选结果继续在既有文章索引中由 URL 恢复，避免在同页堆叠全部文章。 | 搜索实现、配置变量、主题资产和站点输出机制。 |

## 建议的 MorroBlog 排布

桌面端使用“**索引导语 + 主题网格 + 继续阅读轨道**”的三段结构。导语只显示真实标签总数和筛选说明；主题网格使用不对称但规则的卡片尺寸，让第一个真实标签成为视觉锚点，其余标签形成可快速扫视的资料列；每张卡都显示真实文章数、主题名、当前排序号和进入文章索引的箭头。页面底部保留一个“浏览所有文章”的回退路径，而不是伪造热门主题。

手机端取消不对称跨列尺寸，改为连续的紧凑主题行：序号、名称、真实计数和方向箭头保持一行可读，整个行可点击。标签为零时沿用明确的真实空状态，并保留文章索引入口。

## 交互与可访问性边界

标签卡/行应是完整可点击链接，不以仅靠悬停显现的图标承担唯一功能；计数使用真实 `postCount`，无文章的标签仍可进入筛选页并呈现准确的零匹配状态。键盘焦点应清晰可见，移动端不依赖 hover。轻量位移和边框/背景信号可用于指示当前可继续阅读，但需尊重 `prefers-reduced-motion`。

## 下一步

基于以上原则，只改写 `TagsPage.tsx` 及其回归契约和样式；不改变标签数据、文章数量、筛选 URL 或任何内容记录。
