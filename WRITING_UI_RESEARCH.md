# 写作工作台调研笔记

本轮重构参考两个成熟的开源知识编辑器，而非复制其完整产品形态。

| 参考项目 | 可借鉴的交互原则 | MorroBlog 的落地方式 |
| --- | --- | --- |
| [AFFiNE](https://github.com/toeverything/AFFiNE) | 以单一文档画布作为注意力中心；可扩展块与侧栏不应抢占正文；媒体以内容块形式进入文档 | 保持 Markdown 正文为最大、连续的写作画布；将图片上传做成就地插入动作；将元数据移入可收起设置面板 |
| [Outline](https://github.com/outline/outline) | 文档优先、低干扰工具栏、发布与组织信息不打断撰写；内容与附属设置有明确层级 | 顶栏只保留返回、状态、预览、保存草稿和发布；标题与正文处于同一文稿流；分类、标签、摘要、封面等置于文档设置侧栏 |

## 本次确定的界面原则

1. **正文优先。** 首屏进入标题与正文，不再将标题、分类、封面和正文拆成互相争夺注意力的表单区块。
2. **设置可退场。** 文档设置改为桌面端右侧抽屉、移动端底部/折叠面板；写作时默认不占用横向空间。
3. **操作就近。** 预览、插图、字数、草稿状态位于文稿工具栏，避免用户滚动到页面末尾再发布或上传。
4. **状态可感知。** 明示“未保存 / 草稿已保存 / 准备发布”状态，不以营销性按钮替代过程反馈。
5. **保留 MorroBlog 语言。** 使用午夜档案的低饱和墨黑、冰蓝、等宽标注与中文衬线标题；不复制 Notion 或 SaaS 的白底卡片语言。
