# MorroBlog 真实内容发布与公开展示生命周期

## 目标

公开端只展示**管理员实际创建、实际上传或实际发布**的内容。系统不得通过示例文章、示例评论、虚构阅读数据、占位封面或自动填充图片制造内容繁荣。

| 领域 | 允许公开的条件 | 公开端行为 | 管理端行为 |
|---|---|---|---|
| 文章 | 状态为 `published`，并由真实账号创建 | 进入文章列表、时间轴、RSS、sitemap 与相关文章路径 | 草稿、已发布和归档内容均可继续管理 |
| 首页精选 | 管理员保存的 ID 均对应真实、已发布文章 | 设置读取后可作为首页编排依据 | 保存时服务端拒绝不存在或非已发布的文章 ID |
| 图片集 | 至少包含一张实际上传的图片 | 作为带真实封面的卡片显示在 `/gallery` | 空图片集仍保留在后台，方便后续上传和编辑 |
| 图片 | 经 MIME、魔数、二进制大小检查后保存至对象存储 | 仅通过所属图片集公开展示 | 最大原始文件大小为 30 MiB；超限不写入存储 |
| 评论 | 作者真实提交，且状态为 `approved` | 在原文章下公开显示 | 全状态列表可审核、拒绝、退回审核或重新审核 |

## 文章发布闭环

管理员应在控制台中以草稿开始，填写真实标题、slug、正文、摘要、分类、标签与可用封面；确认内容可公开后再切换为 `published`。只有已发布文章可以被保存为首页精选。没有摘要、封面或关联标签时，前台会如实使用“未填写”或空关联状态，而不会生成替代内容。

当前文章删除仍为硬删除，尚未提供回收站、恢复、定时发布、置顶或可见性分级。这些生命周期能力应在后续迁移中单独设计，不能通过隐藏按钮或伪状态提前承诺。

## 图片集策略

管理员可先创建图片集并在后台维护说明，但访客 `/gallery` 只列出已有真实封面图片的集合。没有图片的集合不会以“无封面”卡片占据公开流；当所有集合均为空时，页面显示准确的“暂时没有公开图片集”状态。直接访问空集合详情时仍显示明确空状态，不伪造图片。

## 站点设置策略

站点名称、作者资料、当前状态、社交链接、首页开场、一言回退、精选文章、背景白名单与导航顺序写入 `site_settings` 单例记录。公开端读取此记录；记录不存在或读取失败时，使用代码中稳定的回退配置，保证首页和文章末尾作者卡不因配置缺失而崩溃。

所有写入接口均由服务端管理员权限保护。精选文章必须为真实已发布文章；背景只接受 `/manus-storage/` 对象路径；导航只接受预定义栏目 ID。配置保存不会生成任何文章、评论、用户、图片或图片集。

## 发布前核验

在生产发布前，管理员应从访客身份检查首页、文章索引、时间轴、标签、图片集和单篇文章；从管理员身份检查草稿保存、发布、图片上传、评论审核和站点设置。测试账号、真实内容或真实图片不足时，应保持准确空状态，而不是使用填充数据绕过验收。
