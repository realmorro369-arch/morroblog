# MorroBlog v1.0.0

MorroBlog 的第一个正式发布版本，面向在 fnOS 部署个人技术博客的使用场景。

## 本次包含

- 站内邮箱验证码注册、密码登录、JWT Cookie 会话和独立管理员初始化。
- Markdown 写作、草稿与发布、文章阅读、阅读时长、服务端搜索、分类、标签、归档和嵌套评论。
- 管理后台中的文章、评论审核、术语、图片集和用户管理流程；上传文件通过 S3 存储。
- 以“午夜天文台 × 日系独立技术刊物”为基础的访客优先界面，并提供独立移动端导航与排版节奏。
- 公开一言接口的服务端代理、手动刷新与网络异常回退。
- 经站点所有者确认可用的七首音乐：顺序播放、真实内嵌封面、实时歌词、三层悬浮控制、进度控制和整条拖拽。
- 适配 fnOS 的 `Dockerfile`、`docker-compose.yml` 与 HTTP 局域网 Cookie 兼容策略。

## 安全基线

- `HttpOnly`、HTTPS 下 `Secure`、`SameSite=Lax` 会话 Cookie 与基础安全响应头。
- 验证码有效期、发送频率限制和失败次数限制；密码使用 bcrypt 哈希。
- 图片 Base64、文件大小、声明类型和二进制签名校验；回复评论校验父评论归属。

## 验证

- `pnpm check` 通过。
- Vitest：37 个测试文件、90 项测试通过。
- 真实 SMTP TLS 集成测试默认跳过，需通过 `RUN_SMTP_INTEGRATION=true` 显式启用。

## 已知边界

- Docker 配置已通过构建与部署契约验证；当前开发环境没有 Docker 引擎，未进行容器实跑。
- 深度安全审计尚未完成；后续安全验证会在维护者明确授权后继续，并避免修改真实用户数据或公开内容。

后续开发默认只创建本地 Git 提交；GitHub 将在维护者明确要求发布时再同步。
