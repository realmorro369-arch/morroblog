# morro.asia 视觉逻辑研究：MorroBlog 原创重构边界

**研究日期：** 2026-08-16  
**研究对象：** [morro.asia 首页][1]。  
**目的：** 修正 MorroBlog 过度依赖黑灰面板、弱对比线框和模板化口号的问题。仅提取可迁移的设计逻辑；不复制原站的插画、头像、图标、文字、排版坐标、背景、动画、品牌或页面结构。

## 真实页面观察

| 观察维度 | morro.asia 的呈现方式 | 可迁移的原创原则 |
|---|---|---|
| 背景与明度 | 整页由一张色彩丰富、明度充足的插画承担情绪；黑色只作为局部半透明遮罩，而不是页面默认底色 | MorroBlog 使用深蓝灰作为“夜色”而非纯黑，以低亮纸感表面承托文字；真实观测图只用于一处有意义的视觉锚点 |
| 信息层级 | 问候、身份、短句、时间和网站入口被分散在多个轻量区域，中央留出呼吸空间 | 页面每段只服务一个内容判断：首页先说明“可读内容”，列表先服务“寻找文章”，详情页先服务“读正文” |
| 内容容器 | 信息通过透明度和背景局部暗化获得可读性，而不是使用大量同尺寸、同边框、同阴影的卡片 | 以排版、间距、细分割线和有限的底色变化来组织内容；卡片只用于需独立操作或独立状态的元素 |
| 色彩节奏 | 背景中存在暖橙、褐红、米白与深蓝的自然对比，前景文字以白色为主 | MorroBlog 保留冰蓝作为小面积交互信号，加入低饱和暖纸色作为文本/图片过渡，不扩大为霓虹或彩色系统 |
| 动态信息 | 时钟、天气与问候是简短、可忽略但能带来环境感的次级信息 | 只在已有真实数据时显示次级信息；博客不添加伪造天气、统计或环境状态 |
| 移动优先 | 视觉中心在背景人物与内容组之间保持清晰关系，关键入口数量有限 | 手机端使用一列阅读顺序与可收起的次级操作，避免把桌面卡片栅格缩小后堆叠 |

## MorroBlog 的新视觉决策

> **深色应该像夜里的纸与墨，而不是黑色面板的堆叠。**

重构后的 MorroBlog 将采用更亮的夜蓝基础、暖灰正文、细而不显眼的分隔线，以及从真实图片中提取的有限暖色呼应。顶层 Hero、文章索引和阅读正文不再同时套入厚边框卡片：Hero 以背景光区和排版建立主次，索引用规整的文章行与局部图片提示建立节奏，详情页则优先让标题与正文留在同一连续阅读面上。

本轮不引入任何 morro.asia 的人物图、动漫风格、时钟天气组件、图标、语言或布局坐标。MorroBlog 仍是“午夜天文台 × 日系独立技术刊物”，只是在可读性、明度与留白上改为更接近真实杂志阅读环境的原创版本。

## 可验证目标

| 区域 | 重构标准 |
|---|---|
| 全局 | 正文前景、弱文本与边线均使用可读的非纯黑层次；不再以重复的 `bg-[#202630]` 大卡片作为主要结构 |
| 首页 | 首屏只保留一个有意义的视觉锚点；最近文章以可阅读内容而非“功能卡”出现 |
| 文章索引 | 筛选、结果和文章清单通过网格和分割线组织；移动端筛选默认收起、操作可达 |
| 文章详情 | 标题、元信息、正文和评论保持连续阅读节奏；辅助信息不抢正文注意力 |
| 交付 | 更新视觉契约、桌面与 375×812 截图，并仅创建本地 Git 提交 |

## 运行时观察

2026-08-16 在预览环境直接访问 `/posts` 时，首帧会先显示文章列表骨架；等待数据请求完成后，真实文章 `ddd`、真实“开源”标签、真实“工程实践”分类和 1 篇计数均正常出现。后续视觉验收以完成态为准，不将异步首帧误记为零数据或视觉空状态。

## 参考资料

[1]: https://morro.asia/ "Morro 首页"
